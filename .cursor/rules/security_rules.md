# Security Rules

Source of truth: `docs/ai_lead_intake_bitrix24_tz_v1_0.md`

- Never commit `.env`.
- Never commit webhook URLs.
- Never commit OpenAI API keys.
- Never use real PII.
- Mask email and phone in logs, UI, and screenshots.
- Complete the public repo checklist before publication.
