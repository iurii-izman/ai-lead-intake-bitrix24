# Git Rules

Source of truth: `docs/ai_lead_intake_bitrix24_tz_v1_0.md`

- Branches: `main`, `dev`, `feature/<epic-name>`.
- Commit prefixes: `docs:`, `chore:`, `feat:`, `fix:`, `test:`, `security:`, `refactor:`.
- One epic per branch.
- Before merge, run the checklist and tests.
